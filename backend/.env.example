# Backend Environment Variables

# Security - CHANGE THIS IN PRODUCTION!
SECRET_KEY=secret

# Local Development Mode (non-containerized)
# When running locally with `make dev`, a test user is auto-created:
# Username: test_researcher
# Password: test_password
# Session TTL: 72 hours

# Container Mode (production)
# These are set automatically by Docker:
# SESSION_ID=<unique-session-id>
# SESSION_TTL_HOURS=72
# FRONTEND_PORT=<allocated-port>
# BACKEND_PORT=<allocated-port>
